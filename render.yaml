services:
  - type: web
    name: nestjs-app
    env: node
    plan: free
    buildCommand: npm install && npm run seed && npm run build && npx prisma generate
    startCommand: npm run start:prod
    envVars:
      - key: DATABASE_URL
        sync: false
