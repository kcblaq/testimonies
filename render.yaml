services:
  - type: web
    name: nestjs-app
    env: node
    plan: free
    buildCommand: npm install && npx prisma generate && npm run build
    preDeployCommand: npm run migrate:deploy && npm run seed
    startCommand: npm run start:prod
    envVars:
      - key: DATABASE_URL
        sync: false
